import React, { useState } from "react";
export default function ContactForm({ subject="Website enquiry" }) {
  const [sent, setSent] = useState(false);
  const submit = e => { e.preventDefault(); const f = new FormData(e.currentTarget); const body = `Name: ${f.get("name")}\nEmail: ${f.get("email")}\nPhone: ${f.get("phone")}\n\n${f.get("message")}`; window.location.href = `mailto:info@dvdropincentre.org?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`; setSent(true); };
  return <form onSubmit={submit} className="rounded-3xl bg-white p-6 shadow-xl md:p-9"><div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-bold">Your name<input required name="name" className="mt-2 min-h-12 w-full rounded-xl border px-4 font-normal"/></label><label className="text-sm font-bold">Phone number<input name="phone" type="tel" className="mt-2 min-h-12 w-full rounded-xl border px-4 font-normal"/></label></div><label className="mt-5 block text-sm font-bold">Email address<input required name="email" type="email" className="mt-2 min-h-12 w-full rounded-xl border px-4 font-normal"/></label><label className="mt-5 block text-sm font-bold">How can we help?<textarea required name="message" rows="5" className="mt-2 w-full rounded-xl border p-4 font-normal"/></label><button className="mt-5 min-h-12 rounded-full bg-[#1B5E20] px-7 font-bold text-white transition hover:bg-[#174d1b]">SEND ENQUIRY</button>{sent && <p className="mt-4 text-sm text-[#1B5E20]">Your email app is opening with your enquiry ready to send.</p>}</form>;
}